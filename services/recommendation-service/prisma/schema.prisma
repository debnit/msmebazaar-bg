model UserEvent {
  id           Int      @id @default(autoincrement())
  userId       String
  eventType    String
  listingId    String?
  queryText    String?
  filters      Json?
  ts           DateTime @default(now())
  // Partitioning via raw SQL migrations
}
