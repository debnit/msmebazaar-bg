import React from 'react';
import { View, Text } from 'react-native';
import { AuthLayoutProps } from './index';

const AuthLayoutMobile: React.FC<AuthLayoutProps> = (props) => {
  return (
    <View>
      <Text>AuthLayout - Mobile Implementation</Text>
    </View>
  );
};

export default AuthLayoutMobile;
