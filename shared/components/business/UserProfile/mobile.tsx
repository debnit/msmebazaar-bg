import React from 'react';
import { View, Text } from 'react-native';
import { UserProfileProps } from './index';

const UserProfileMobile: React.FC<UserProfileProps> = (props) => {
  return (
    <View>
      <Text>UserProfile - Mobile Implementation</Text>
    </View>
  );
};

export default UserProfileMobile;
