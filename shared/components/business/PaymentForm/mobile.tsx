import React from 'react';
import { View, Text } from 'react-native';
import { PaymentFormProps } from './index';

const PaymentFormMobile: React.FC<PaymentFormProps> = (props) => {
  return (
    <View>
      <Text>PaymentForm - Mobile Implementation</Text>
    </View>
  );
};

export default PaymentFormMobile;
