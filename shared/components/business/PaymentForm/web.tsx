import React from 'react';
import { PaymentFormProps } from './index';

const PaymentFormWeb: React.FC<PaymentFormProps> = (props) => {
  return (
    <div>
      <h1>PaymentForm - Web Implementation</h1>
    </div>
  );
};

export default PaymentFormWeb;
