import React from 'react';
import { View, Text } from 'react-native';
import { ModalProps } from './index';

const ModalMobile: React.FC<ModalProps> = (props) => {
  return (
    <View>
      <Text>Modal - Mobile Implementation</Text>
    </View>
  );
};

export default ModalMobile;
