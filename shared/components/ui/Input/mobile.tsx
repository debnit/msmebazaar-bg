import React from 'react';
import { View, Text } from 'react-native';
import { InputProps } from './index';

const InputMobile: React.FC<InputProps> = (props) => {
  return (
    <View>
      <Text>Input - Mobile Implementation</Text>
    </View>
  );
};

export default InputMobile;
