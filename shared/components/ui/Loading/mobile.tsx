import React from 'react';
import { View, Text } from 'react-native';
import { LoadingProps } from './index';

const LoadingMobile: React.FC<LoadingProps> = (props) => {
  return (
    <View>
      <Text>Loading - Mobile Implementation</Text>
    </View>
  );
};

export default LoadingMobile;
