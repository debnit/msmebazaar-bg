import React from 'react';
import { LoadingProps } from './index';

const LoadingWeb: React.FC<LoadingProps> = (props) => {
  return (
    <div>
      <h1>Loading - Web Implementation</h1>
    </div>
  );
};

export default LoadingWeb;
