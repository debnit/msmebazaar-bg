{
  "name": "@msmebazaar/shared",
  "version": "1.0.0",
  "description": "Shared utilities, types, and configurations for MSMEBazaar platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "eslint '**/*.{ts,tsx}'",
    "test": "jest",
    "prepare": "npm run build",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "zod": "^3.25.67",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.0",
    "@types/express": "^5.0.3",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.50.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
